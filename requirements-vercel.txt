# ── Vercel-only dependencies ──────────────────────────────────────────────────
# Lightweight subset of requirements.txt for Vercel serverless deployment.
# Excludes heavy ML packages (torch, sentence-transformers, faiss-cpu,
# scikit-learn) and browser automation (playwright) that exceed the 500 MB
# Vercel function size limit.
#
# The full requirements.txt is still used for Docker / container deployments.

# ── Resume parsing ────────────────────────────────────────────────────────────
PyMuPDF==1.24.5
openai>=1.0.0
python-dotenv==1.0.1

# ── Database ──────────────────────────────────────────────────────────────────
psycopg[binary]>=3.1
psycopg-pool>=3.1

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# ── Scraping (lightweight only; playwright excluded) ──────────────────────────
requests>=2.31
beautifulsoup4>=4.12
APScheduler>=3.10

# ── Storage & messaging ──────────────────────────────────────────────────────
boto3>=1.34
kafka-python>=2.0.2
